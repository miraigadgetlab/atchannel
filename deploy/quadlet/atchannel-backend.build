# @channel podman build unit for the backend image
# Build context = repo root; File points at the shared Dockerfile.
[Unit]
Description=Build @channel backend image

[Build]
ImageTag=localhost/atchannel-backend
File=/opt/atchannel/deploy/Dockerfile.backend
SetWorkingDirectory=/opt/atchannel

[Service]
Type=oneshot
